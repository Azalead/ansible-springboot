[Unit]
Description={{ springboot_application_name }}
After=syslog.target

[Service]
User="{{ springboot_user }}"
Restart=on-failure
ExecStart="{{ springboot_deploy_folder }}/{{ springboot_application_name }}.{{ springboot_application_type }}"
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
